service types
===============

NodePort - makes a pod from node available with the help of a port 
ClusterIP - Virtual IP inside the cluster
LoadBalancer -


spec:
  type: NodePort
  ports:
     - targetPort:
       port:
       nodePort:
  selector:
        tier: frontend
       
kubectl get svc

spec:
   type: ClusterIP
   ports:
      - targetPort: 
        port:
   
   selector:
         app: selector
         
Ingress - helps you to access your service using a url . 

Ingress is configured as - Ingress Controller (Nginx,HAProxy) + Ingress Resources (Configuration)
